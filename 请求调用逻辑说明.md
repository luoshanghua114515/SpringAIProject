# SkyTakeOut AI — 请求调用逻辑完整说明

## 文档概述

本文档详细描述 Spring AI 项目中一次用户请求从进入系统到返回响应的完整调用链，涵盖拦截器链、Controller 层、Agent 路由分发、复杂任务计划执行、工具调用、SSE 流式响应、记忆持久化等各个环节。

---

## 一、整体架构概览

### 1.1 技术栈

| 组件 | 技术选型 |
|---|---|
| 框架 | Spring Boot 3.5 + Spring AI 1.1.6 |
| 对话模型 | DeepSeek V4 Flash（兼容 OpenAI SDK） |
| 向量嵌入模型 | text-embedding-v4（阿里云百炼） |
| 关系数据库 | MySQL（业务数据：菜品、订单、用户等） |
| 缓存 | Redis + Lettuce（登录态、查询缓存、会话管理） |
| 分布式锁 | Redisson（缓存击穿防护） |
| 向量存储 | SimpleVectorStore（内存级，RAG 问答） |
| 通信协议 | SSE（Server-Sent Events）流式推送 |
| 序列化 | Kryo（对话记忆文件持久化） |

### 1.2 核心组件分层

```
┌─────────────────────────────────────────────────┐
│                  用户请求入口                      │
│     GET /ai/chat/{msg}  + 请求头 (authorization,  │
│     chatId)                                      │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│              拦截器链 (Interceptor)                │
│  1. RefreshTokenInterceptor (order=0, 全局)      │
│     → 从 Redis 恢复用户信息、刷新 token 有效期      │
│  2. LoginInterceptor (order=1, /ai/chat/**)     │
│     → 校验 BaseContent.getUser() 非空             │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│             Controller 层                         │
│  ChatController.serviceChat()                    │
│  → 创建 SseEmitter (10min 超时)                  │
│  → 捕获主线程 RequestAttributes                  │
│  → CompletableFuture.runAsync() 异步执行          │
│  → 传递 Web 线程上下文到异步线程                   │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│            RouterAgent (路由分发)                  │
│  1. selectSkillsWithLLM() — 匹配业务技能          │
│  2. llmClassify() — 意图分类                      │
│     ├── SIMPLE_CHAT  → SimpleChatAgent            │
│     ├── COMPLEX_TASK → PlanExecute + 多 Agent 执行 │
│     └── AMBIGUOUS    → 直接反问用户                │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│              结果处理 & 持久化                      │
│  1. SSE 流式推送思考过程和最终结果                  │
│  2. FileBasedChatMemory 保存对话记录 (Kryo)       │
│  3. Redis 缓存会话有效期 (24h)                     │
└─────────────────────────────────────────────────┘
```

---

## 二、请求入口与拦截器链

### 2.1 HTTP 请求示例

```
GET /ai/chat/我想点一份红烧肉 HTTP/1.1
Host: localhost:8080
authorization: eyJhbGciOiJIUzI1NiIs...
chatId: session_1718524800
```

### 2.2 第一层：RefreshTokenInterceptor（全局拦截器）

**文件**: `src/main/java/com/kanodays88/skytakeoutai/interceptor/RefreshTokenInterceptor.java`

**注册路径**: `/**`，order = 0（最先执行）

**职责**: 对所有请求进行 token 刷新，不做实际拦截

**执行逻辑**:

```
1. 从请求头获取 authorization token
   → request.getHeader("authorization")

2. 根据 token 从 Redis 查询用户信息
   → stringRedisTemplate.opsForHash().entries("login:user:" + token)

3. 如果 Redis 中存在用户信息：
   a. 将 Map 数据转为 UserLoginDTO 对象
      → BeanUtil.fillBeanWithMap(userMap, new UserLoginDTO(), false)
   b. 存入 ThreadLocal
      → BaseContent.setUser(userLoginDTO)
   c. 刷新 token 过期时间（60 分钟）
      → stringRedisTemplate.expire("login:user:" + token, 60, TimeUnit.MINUTES)

4. 始终返回 true（放行），不拦截任何请求
```

**关键设计意图**: 用户在访问无需登录的页面时也能自动刷新 token，避免因长时间不访问受保护页面导致 token 过期掉线。

**请求完成后清理**: `afterCompletion()` 方法中调用 `BaseContent.removeUser()` 清理 ThreadLocal，防止内存泄漏。

### 2.3 第二层：LoginInterceptor（登录校验拦截器）

**文件**: `src/main/java/com/kanodays88/skytakeoutai/interceptor/LoginInterceptor.java`

**注册路径**: `/ai/chat/**`，order = 1

**排除路径**: `/ai/chat/test`（测试接口免登录）、`/ai/user/login`（登录接口免登录）、`/upload/**`（静态资源免登录）

**执行逻辑**:

```
1. 检查 ThreadLocal 是否有用户信息
   → BaseContent.getUser()

2. 如果为 null（未登录）：
   → response.setStatus(401)
   → log.info("拦截，用户未登录")
   → return false (拦截请求)

3. 如果不为 null（已登录）：
   → return true (放行)
```

### 2.4 拦截器注册配置

**文件**: `src/main/java/com/kanodays88/skytakeoutai/config/MVCConfig.java`

```java
@Override
public void addInterceptors(InterceptorRegistry registry) {
    // order(0): 全局 token 刷新拦截器，优先执行
    registry.addInterceptor(refreshTokenInterceptor)
            .addPathPatterns("/**")
            .order(0);

    // order(1): 登录校验拦截器，后执行
    registry.addInterceptor(loginInterceptor)
            .addPathPatterns("/ai/chat/**")
            .excludePathPatterns("/ai/chat/test", "/ai/user/login", "/upload/**")
            .order(1);
}
```

**执行顺序**: order 越小优先级越高，所以 `RefreshTokenInterceptor` 先执行（先刷新 token），`LoginInterceptor` 后执行（再校验登录态）。

---

## 三、ChatController 核心聊天接口

### 3.1 接口定义

**文件**: `src/main/java/com/kanodays88/skytakeoutai/controller/ChatController.java`

**主聊天接口**:

```java
@RequestMapping(value = "/{msg}", produces = "text/event-stream;charset=UTF-8")
public SseEmitter serviceChat(
        @PathVariable("msg") String msg,
        @RequestHeader("chatId") String chatId
)
```

| 参数 | 来源 | 说明 |
|---|---|---|
| `msg` | URL 路径变量 | 用户输入的文本消息（中文无需 URL 编码问题因 Spring Boot 自动处理） |
| `chatId` | 请求头 | 会话 ID，用于区分不同对话 |

**其他接口**:

| 接口 | 方法 | 功能 |
|---|---|---|
| `/ai/chat/test` | GET | 测试接口，免登录 |
| `/ai/chat/history` | GET | 获取用户所有历史会话 ID 列表 |
| `/ai/chat/history/{chatId}` | GET | 获取指定会话的完整对话记录 |
| `/ai/chat/history/remove/{chatId}` | DELETE | 删除指定会话记录 |
| `/ai/chat/rag/{msg}` | GET | RAG 问答接口（基于 PDF 文档的知识库问答） |

### 3.2 主聊天接口执行流程

#### 阶段 1：同步预处理（主线程）

```
1. 捕获当前 Web 线程的 RequestAttributes
   → RequestAttributes attributes = RequestContextHolder.getRequestAttributes()

2. 获取当前线程的登录用户信息
   → UserLoginDTO userLoginDTO = BaseContent.getUser()

3. 创建 SSE Emitter，设置 10 分钟超时
   → SseEmitter emitter = new SseEmitter(10 * 60 * 1000L)

4. 启动异步线程处理全部业务逻辑
   → CompletableFuture.runAsync(() -> { ... })
```

#### 阶段 2：异步上下文初始化（子线程）

```
1. 将主线程的 RequestAttributes 传递到子线程
   → RequestContextHolder.setRequestAttributes(attributes)
   作用：确保子线程能访问 HttpServletRequest 信息

2. 设置当前线程的会话 ID
   → BaseContent.setChatId(chatId)

3. 设置当前线程的登录用户
   → BaseContent.setUser(userLoginDTO)

4. 创建文件级对话记忆实例
   → FileBasedChatMemory fileBasedChatMemory =
        new FileBasedChatMemory("tmp/{username}/chatMemory/")
```

#### 阶段 3：意图路由（核心分发逻辑）

```java
// 创建路由智能体
RouterAgent routerAgent = new RouterAgent(
    openAiChatModel,    // DeepSeek 对话模型
    vectorStore,        // 向量存储
    allTools,           // 全部工具回调数组
    skillRegistry,      // 技能注册中心
    emitter             // SSE 发射器（用于流式推送思考过程）
);

// 执行路由，获取决策结果
RouteDecisionTotal route = routerAgent.route(msg, chatId);
```

#### 阶段 4：按路由结果分派处理

```java
if (route.decision().questionType() == QuestionType.SIMPLE_CHAT) {
    // 路径一：简单对话
    SimpleChatAgent simpleChatAgent = new SimpleChatAgent(openAiChatModel, allTools);
    String s = simpleChatAgent.simpleChat(msg, chatId);
    SSESend.sendEventResult(emitter, s);
    aiResult = s;

} else if (route.decision().questionType() == QuestionType.COMPLEX_TASK) {
    // 路径二：复杂任务
    String planResult = planExecute.planExecute(
        route.decision().mianTask(), chatId, emitter);
    SSESend.sendEventResult(emitter, planResult);
    aiResult = planResult;

} else if (route.decision().questionType() == QuestionType.AMBIGUOUS) {
    // 路径三：意图不明确，反问用户
    String question = route.decision().returnQuestion();
    SSESend.sendEventResult(emitter, question);
    aiResult = question;
}
```

#### 阶段 5：对话记忆持久化

```
1. 将本次用户提问和 AI 回复存入文件记忆
   → fileBasedChatMemory.add(chatId,
        List.of(new UserMessage(msg), new AssistantMessage(aiResult)))

2. 在 Redis 中记录会话最后活跃时间（用于后续过期清理）
   → stringRedisTemplate.opsForValue().set(
        "chatMemory:user:chatId",
        JSONUtil.toJsonStr(LocalDateTime.now().plusHours(24)))

3. 将会话键加入待清理集合
   → stringRedisTemplate.opsForSet().add("remove:chatMemory", chatMemoryCache)
```

#### 阶段 6：异常处理与资源清理

```java
catch (Exception e) {
    log.error("执行异常：{}", e.getMessage());
    SSESend.sendEventResult(emitter, "执行失败: " + e.getMessage());
    emitter.completeWithError(e);
} finally {
    BaseContent.removeChatId();                    // 清理 ThreadLocal
    RequestContextHolder.resetRequestAttributes();  // 释放 Web 上下文
    emitter.complete();                             // 关闭 SSE 连接
}
```

---

## 四、RouterAgent 意图路由与技能匹配

### 4.1 RouterAgent 概述

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/router/RouterAgent.java`

RouterAgent 是请求分发的中枢，职责如下：

1. **技能匹配**（`selectSkillsWithLLM`）：根据用户输入，通过 LLM 语义匹配业务技能
2. **意图分类**（`llmClassify`）：综合技能匹配结果和对话历史，将问题分为三类
3. **决策输出**：返回 `RouteDecisionTotal`，包含分类结果、理由、匹配的技能列表

### 4.2 技能匹配流程

#### 调用入口

```java
List<String> selectedSkillNames = selectSkillsWithLLM(userPrompt);
```

#### 执行逻辑

```
1. 从 SkillRegistry 获取所有技能摘要：
   - skillName: description

2. 从 FileBasedChatMemory 获取当前会话历史

3. 构建 LLM Prompt：
   "根据用户输入和历史对话，从以下可用技能中选择最匹配的业务技能：
    {skills}
    选择原则：仅基于技能名称和描述进行语义匹配。"

4. 调用 LLM，通过 BeanOutputConverter 解析 JSON 输出：
   {"skillNames": ["food_delivery"]}

5. 根据匹配的技能名称从 SkillRegistry 加载完整技能定义
   → Skill 对象包含：参数表、执行步骤、工具列表、参考文档等
```

#### 技能上下文内容（注入下游 LLM 调用）

```java
String skillContext = skillRegistry.getSkillsPromptContext(skills);
```

每个技能的上下文包含：
- 技能名称与领域
- 业务流程描述
- 参数列表（含"必需"或"可选"标注）
- 执行步骤列表
- 关联的工具名称
- 示例提问

### 4.3 意图分类流程

#### 核心 Prompt 结构

```java
String systemPrompt = """
    【当前工作】
    你是一个问题分类专家，判断问题类型

    【匹配到以下业务技能】
    {skillContext}

    【分类规则（优先级从高到低）】
    1. 匹配到业务技能：
       - 信息充分 → COMPLEX_TASK
       - 缺少必需信息 → AMBIGUOUS
    2. 无技能但需多工具 → COMPLEX_TASK
    3. 问题模糊不清 → AMBIGUOUS
    4. 单工具或简单问答 → SIMPLE_CHAT

    【输出参数】
    questionType: COMPLEX_TASK / AMBIGUOUS / SIMPLE_CHAT
    reason: 判断理由
    returnQuestion: 反问用户的问题（仅 AMBIGUOUS 时有值）
    mainTask: 详细总任务描述
    """
```

#### 关键技术点：禁用自动工具执行

```java
ChatOptions chatOptions = ToolCallingChatOptions.builder()
        .internalToolExecutionEnabled(false)  // 禁止 Spring AI 内部自动执行工具
        .build();
```

RouterAgent 在分类阶段只需要 LLM 判断工具需求，**绝不实际调用任何工具**。

#### 三种分类结果

| 分类 | 含义 | 典型场景 |
|---|---|---|
| `SIMPLE_CHAT` | 简单对话 | 闲聊问候、单步查询、从历史对话可回答 |
| `COMPLEX_TASK` | 复杂任务 | 多步骤业务流程（查询菜品 → 下单）、需多工具协作 |
| `AMBIGUOUS` | 意图不明确 | 缺少必需信息（如用户只说"我想点餐"但没有具体内容） |

---

## 五、三种任务处理路径详解

### 5.1 路径一：SIMPLE_CHAT — SimpleChatAgent

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/simpleChat/SimpleChatAgent.java`

#### 执行流程

```
1. 加载历史对话
   → List<Message> messages = chatMemory.get(chatId)

2. 构建 Prompt（含历史记忆 + 系统人设 + 用户问题 + 工具集）

3. 调用 LLM
   → chatClient.prompt(historyChat)
        .system(ChatSystem.CHAT_SYSTEM)  // 永雏塔菲人设
        .user(userPrompt)
        .toolCallbacks(allTools)           // 全部 10 个工具
        .call()
        .content()

4. 返回 LLM 回复文本
```

#### ChatSystem 系统提示词

**文件**: `src/main/java/com/kanodays88/skytakeoutai/common/ChatSystem.java`

- 角色：B站虚拟主播「永雏塔菲」，永远17岁的粉发金瞳少女
- 风格要求：句末带"喵"、软萌口吻、傲娇小恶魔性格
- 核心约束：不得捏造数据、必须以工具查询结果为准
- 展示要求：菜品/套餐推荐使用表格形式（含图片链接）

---

### 5.2 路径二：COMPLEX_TASK — PlanExecute 执行引擎

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/plan/PlanExecute.java`

#### 完整执行链路

```
用户总任务 (mainTask)
    │
    ▼
┌─────────────────────────────────────┐
│ Step 1: 任务分解                    │
│ decomposeTaskWithContract(task)     │
│ → LLM 将总任务分解为多个子任务       │
│ → 每个子任务包含依赖关系和核心输出    │
└──────────────────────┬──────────────┘
                       ▼
┌─────────────────────────────────────┐
│ Step 2: 构建执行波次                 │
│ buildExecutionWaves(subTasks)       │
│ → 拓扑排序，构建多级 Wave           │
│ → 同一 Wave 内的任务可并行执行       │
└──────────────────────┬──────────────┘
                       ▼
┌─────────────────────────────────────┐
│ Step 3: 逐 Wave 并行执行子任务       │
│ for each Wave:                      │
│   ExecutorService 线程池             │
│   CompletableFuture.runAsync() × N   │
│   → 每个任务创建 Kanodays88Manus     │
│   → ReAct 循环（思考→工具调用）       │
│   → 结果蒸馏                        │
│   → CompletableFuture.allOf().join()│
└──────────────────────┬──────────────┘
                       ▼
┌─────────────────────────────────────┐
│ Step 4: 结果融合                    │
│ fuseResults(task, subTaskResults)   │
│ → 整合所有蒸馏后的子任务结果         │
│ → LLM 生成最终回复                  │
└──────────────────────┬──────────────┘
                       ▼
                   返回结果
```

#### Step 1：任务分解 (decomposeTaskWithContract)

**输入**: 用户总任务描述（来自 RouterAgent 的 `mainTask` 字段）

**Prompt**:

```
生成带依赖契约的结构化结果，严格遵守规则：
1. 为每个子任务定义完整契约：
   - taskId：子任务序号，从1开始递增
   - taskName：子任务名称
   - taskContent：子任务具体执行内容
   - downstreamTaskIds：所有会用到该子任务结果的下游任务ID
   - coreContent：所有下游任务要求该子任务必须输出的核心内容
   - toolNames：子任务执行需要调用的工具名称
2. 子任务依赖关系必须清晰
3. 不得生成无工具的任务
4. 不得生成总结类任务（fuseResults 负责最终总结）
```

**输出示例**（LLM 返回的结构化 JSON）：

```json
{
  "subTaskList": [
    {
      "taskId": 1,
      "taskName": "查询菜品信息",
      "taskContent": "根据用户要求查询红烧肉的价格、图片和口味信息",
      "downstreamTaskIds": [2],
      "coreContent": ["菜品名称", "价格", "图片链接"],
      "toolNames": ["dishTool"]
    },
    {
      "taskId": 2,
      "taskName": "创建订单",
      "taskContent": "使用菜品信息和用户地址创建订单",
      "downstreamTaskIds": [],
      "coreContent": ["订单号", "总金额", "配送地址"],
      "toolNames": ["orderTool"]
    }
  ]
}
```

#### Step 1 数据模型：SubTask

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/plan/SubTask.java`

```java
public record SubTask(
    int taskId,                    // 子任务序号（全局唯一）
    String taskName,               // 子任务名称
    String taskContent,            // 子任务执行内容
    Set<Integer> downstreamTaskIds,// 所有依赖该子任务的下游任务ID
    Set<String> coreContent,       // 下游任务要求必须输出的核心内容
    Set<String> toolNames          // 该任务需要调用的工具
) {}
```

#### Step 2：构建执行波次 (buildExecutionWaves)

**算法**: 基于 DAG（有向无环图）的拓扑排序

```
以 {taskId → downstreamTaskIds} 构建依赖关系：
- 如果任务 B 的 downstreamTaskIds 包含任务 A 的 taskId
  则 A 依赖 B（B 是 A 的上游）

拓扑分层：
- Wave 1: 没有上游依赖的任务（无入边）
- Wave n: 依赖 Wave n-1 的任务

示例：
  任务1（查询菜品）→ 任务2（创建订单）→ 任务3（发送通知）
  Wave 1: [任务1]
  Wave 2: [任务2]
  Wave 3: [任务3]

  任务1（查询菜品）\            → 任务3（生成报告）
  任务2（查询天气）/  ← Wave 1: [任务1, 任务2] 可并行
  Wave 2: [任务3]
```

**循环依赖兜底**: 如果出现所有剩余任务都有依赖的情况，将剩余任务全部放入同一个 Wave 执行，防止死锁。

#### Step 3：并行执行子任务

**线程上下文传递**:

```java
// 主线程捕获
RequestAttributes attributes = RequestContextHolder.getRequestAttributes();
UserLoginDTO userLoginDTO = BaseContent.getUser();

// 每个异步子线程中恢复
RequestContextHolder.setRequestAttributes(attributes);
BaseContent.setChatId(chatId);
BaseContent.setUser(userLoginDTO);
```

**执行单元创建**:

```java
// 1. 获取子任务需要的工具（按 toolNames 过滤）
ToolCallback[] tools = getTools(task.toolNames());

// 2. 创建独立的 Manus 智能体实例
Kanodays88Manus evelynManus = new Kanodays88Manus(tools, openAiChatModel);

// 3. 填充上游任务的蒸馏结果作为上下文
String upStreamTaskResult = checkAndFillUpstreamContext(task, resultMap);
evelynManus.setMessageList(
    List.of(new SystemMessage(upStreamTaskResult))
);

// 4. 执行 ReAct 循环
List<String> childResult = evelynManus.run(
    task.taskContent(), task.taskName(), emitter);
```

**结果蒸馏**:

```java
if (result.length() < 2000) {
    // 短结果直接复用，跳过蒸馏
    distilledResult = new DistilledResult(taskId, result, result, task);
} else {
    // 长结果执行 LLM 蒸馏压缩
    distilledResult = distillSubTaskResult(task, result);
}
```

**蒸馏 Prompt**:

```
对用户输入的子任务的原始结果做蒸馏，严格遵守以下规则：
1. 不得删除下游任务需要的所有核心内容：{coreContent}
2. 删除所有与核心内容无关的内容
```

**蒸馏数据模型**:

```java
record DistilledResult(
    int taskId,                    // 所属子任务ID
    String structuredCoreResult,   // 【进上下文】结构化蒸馏结果
    String rawResult,              // 【归档】原始完整结果（兜底召回）
    SubTask subTask                // 子任务契约
) {}
```

**波次同步**: `CompletableFuture.allOf(futures).join()` 确保当前 Wave 的所有任务全部完成后才进入下一个 Wave。

#### Step 4：结果融合 (fuseResults)

**输入**: 全部子任务的 `structuredCoreResult`（按 taskId 排序）

**Prompt**:

```
{role}
基于以下子任务的执行结果，整合成最终完整的任务报告返回给用户。
不要遗漏任何子任务的关键结果。
【核心目标】：{mainGoal}
【子任务结果列表】：
{subTaskResults}
```

**输出**: LLM 生成的最终用户回复。

---

### 5.3 路径三：AMBIGUOUS — 反问用户

当用户输入缺少关键信息时，RouterAgent 会在 `RouteDecision.returnQuestion` 中生成具体反问内容：

```java
String question = route.decision().returnQuestion();
SSESend.sendEventResult(emitter, question);
```

**示例**:

```
用户: "我想点餐"
AI:   "请问您想点什么菜品？需要几份？配送到什么地址？"
```

---

## 六、智能体系统层次结构

### 6.1 类继承体系

```
BaseAgent (抽象基类)
  │
  └── ReActAgent (抽象：定义 think/act 模式)
        │
        └── ToolCallAgent (工具调用能力：禁用自动执行，手动编排)
              │
              ├── Kanodays88Manus (主执行智能体，5 步最大)
              │
              └── RAGAgent (RAG 问答智能体)
```

### 6.2 BaseAgent 基类

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/BaseAgent.java`

| 字段 | 说明 |
|---|---|
| `name` | 智能体名称 |
| `state` | 状态：IDLE / RUNNING / FINISHED / ERROR |
| `maxSteps` | 最大执行步数（默认 5） |
| `currentStep` | 当前执行步数 |
| `messageList` | ReAct 循环记忆消息列表 |
| `chatClient` | ChatClient 实例 |
| `nextStepPrompt` | 下一步提示词（卡死恢复用） |

**核心方法 `run()`**:

```java
public List<String> run(String userPrompt, String taskName, SseEmitter emitter) {
    // 1. 校验状态和输入
    // 2. 设置状态为 RUNNING
    // 3. 添加用户消息到记忆
    // 4. 循环执行 step()，最多 maxSteps 次
    // 5. 每次循环检测卡死状态
    // 6. 超步数自动结束
    // 7. 异常处理
    // 8. finally 清理状态和记忆
}
```

**卡死检测** (`isStuck()`): 检测最近 5 条消息中是否有 3 次以上重复输出，重复则触发 `handleStuckState()`，注入"切换策略"提示。

### 6.3 ReActAgent

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/ReActAgent.java`

定义 ReAct 模式（思考 → 行动循环）：

```java
public abstract boolean think(String userPrompt, String taskName, SseEmitter sseEmitter);
public abstract String act();

public String step(String userPrompt, String taskName, SseEmitter sseEmitter) {
    boolean think = think(userPrompt, taskName, sseEmitter);
    if (!think) return "思考完成-无需调用工具";
    return act();
}
```

### 6.4 ToolCallAgent

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/ToolCallAgent.java`

#### think() — 思考阶段

```
1. 加载系统提示词（含当前步骤/总步数、任务描述）
2. 加载记忆消息列表（历史思考 + 工具调用结果）
3. 调用 LLM，获取 ChatResponse
4. 从 response metadata 提取 reasoningContent（思考过程）
5. 通过 SSE 推送思考过程到前端
6. 判断 LLM 是否返回了工具调用：
   - 有工具调用 → 返回 true（进入 act）
   - 无工具调用 → 返回 false（结束思考）
```

**系统提示词**:

```
角色：使用工具专注完成当前任务的AI助手
核心规则：
1. 只能使用提供的工具完成当前任务
2. 当判断任务完成或无法继续时，立即调用 assignmentFinish 工具
3. 当前剩余思考次数：{now} / {max}
4. 仅可输出调用工具的信息，不得输出其他结论
当前任务：{question}
```

#### act() — 行动阶段

```
1. 检查是否有工具调用，无则直接返回
2. 调用 ToolCallingManager.executeToolCalls()
   → 自动执行 LLM 请求的所有工具
   → 返回完整的对话历史（含工具调用和工具响应）
3. 处理 reasoningContent 元数据（嵌入 AssistantMessage）
4. 更新智能体的记忆消息列表
5. 检查是否有关闭工具 (assignmentFinish) 被调用
   → 是则设置状态为 FINISHED
6. 返回工具执行结果摘要
```

**关键设计**: `ToolCallingChatOptions.internalToolExecutionEnabled = false`，禁用 Spring AI 的自动工具执行，由 ToolCallAgent 手动通过 `ToolCallingManager` 编排执行，实现对工具调用过程的精细控制。

### 6.5 Kanodays88Manus

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/Kanodays88Manus.java`

- 最大执行步数：5 步
- 继承 `ToolCallAgent`，配置特定的 ChatClient（含 MyLoggerAdvisor 日志切面）
- 作为 PlanExecute 中每个子任务的执行单元

---

## 七、SSE 流式响应机制

### 7.1 SSESend 工具类

**文件**: `src/main/java/com/kanodays88/skytakeoutai/agent/sse/SSESend.java`

提供两种静态方法：

| 方法 | SSE 事件类型 | 用途 | 前缀 |
|---|---|---|---|
| `sendEventThink()` | `think`（自定义事件名） | 推送 AI 思考过程 | "Agent思考:" |
| `sendEventResult()` | `result`（自定义事件名） | 推送最终结果 | "Agent结果:" |

**线程安全**: 使用 `synchronized(emitter)` 保证并发安全。

**UTF-8 强制**: 通过 `MediaType(TEXT_PLAIN, StandardCharsets.UTF_8)` 确保中文不乱码。

### 7.2 SSE 推送时机

在整个请求生命周期中，SSE 推送发生在以下节点：

| 阶段 | 事件类型 | 推送内容示例 |
|---|---|---|
| RouterAgent 意图分析 | think | "正在进行意图分析..." |
| RouterAgent 返回分类 | think | "匹配到外卖配送技能，用户提供了菜品名称，信息充分" |
| PlanExecute 任务分解 | think | "开始对任务进行拆分..." |
| PlanExecute 拆分完成 | think | "任务拆分完成：\n任务1：查询菜品信息\n---\n任务2：创建订单" |
| 每个子任务开始 | think | "开始执行任务【查询菜品信息】" |
| ToolCallAgent.think() | think | LLM 的 reasoningContent（思考链） |
| 最终结果 | result | "Agent结果:已为您查询到以下菜品..." |
| 异常情况 | result | "Agent结果:执行失败: ..." |

### 7.3 SSE Emitter 生命周期

```
创建: new SseEmitter(10 * 60 * 1000L)   10分钟超时
  │
  ▼
多次发送: sendEventThink() / sendEventResult()
  │
  ├── 正常完成: emitter.complete()
  └── 异常关闭: emitter.completeWithError(e)
```

---

## 八、技能系统（Skill System）

### 8.1 架构概览

**文件**:

- `Skill.java` — 技能数据模型
- `SkillParameter.java` — 技能参数定义
- `SkillStep.java` — 技能执行步骤定义
- `SkillLoader.java` — 技能 Markdown 文件解析加载器
- `SkillRegistry.java` — 技能注册中心（查找、匹配、上下文生成）

### 8.2 技能定义文件

所有技能定义位于 `src/main/resources/skills/` 目录下：

```
skills/
├── food_delivery/
│   ├── query_dish_or_setmeal/
│   │   ├── SKILL.md          # 菜品/套餐查询技能
│   │   └── references/
│   │       └── dish-flavor.md # 菜品口味参考数据
│   ├── place_order/
│   │   └── SKILL.md          # 下单技能
│   ├── cancel_order/
│   │   └── SKILL.md          # 取消订单技能
│   └── query_order/
│       └── SKILL.md          # 查询订单技能
└── tarvel_plan/
    └── make_tarvel_plan/
        └── SKILL.md          # 旅游行程规划技能
```

### 8.3 技能在请求流程中的使用位置

```
RouterAgent 阶段：
  1. selectSkillsWithLLM()
     → 读取 SkillRegistry 中所有技能的 name + description
     → LLM 匹配最相关技能
  2. llmClassify()
     → 将匹配技能的完整定义注入 Prompt
     → LLM 根据技能参数表判断信息完整性
     → 决定 COMPLEX_TASK 或 AMBIGUOUS

PlanExecute 阶段：
  3. decomposeTaskWithContract()
     → 匹配技能的定义决定了子任务划分方式
     → 技能的流程步骤映射为 SubTask 列表

ToolCallAgent 阶段：
  4. LoadReferenceTool
     → Agent 执行过程中调用工具加载技能参考文档
```

---

## 九、RAG 问答流程

### 9.1 接口入口

```java
@RequestMapping(value = "/rag/{msg}", produces = "text/event-stream;charset=UTF-8")
public SseEmitter rag(@PathVariable("msg") String userMessage,
                      @RequestHeader("chatId") String chatId)
```

### 9.2 执行流程

```
1. 用户上传 PDF 文件
   → FileUploadController.uploadPdf()
   → FileUploadServiceImpl.save() 保存文件
   → 后续通过 DocumentLoader 加载并向量化

2. 用户提问（RAG 接口）
   → ChatController.rag()
   → VectorStore.similaritySearch() 检索相关片段
     - 检索条件：user == 当前用户 AND chat_id == 当前会话
     - topK = 5（返回最相关的 5 个文档片段）

3. 构建增强 Prompt
   → "根据额外信息回答用户提问，当额外信息为null时，提醒用户没有上传pdf文件"

4. SimpleChatAgent 处理
   → 加载历史记忆
   → 调用 LLM 生成回答
   → 返回结果
```

### 9.3 过滤表达式

```java
FilterExpressionBuilder filter = new FilterExpressionBuilder();
Filter.Expression expression = filter
    .and(filter.eq("user", BaseContent.getUser().getUserName()),
         filter.eq("chat_id", chatId))
    .build();
```

确保 RAG 检索只返回当前用户在当前会话中上传的文档内容。

---

## 十、记忆系统与数据持久化

### 10.1 FileBasedChatMemory（文件级对话记忆）

**文件**: `src/main/java/com/kanodays88/skytakeoutai/memory/FileBasedChatMemory.java`

**技术选型**: Kryo 序列化（高性能二进制序列化）

**存储路径**: `./tmp/{username}/chatMemory/{chatId}.kryo`

**核心方法**:

| 方法 | 功能 |
|---|---|
| `add(conversationId, messages)` | 追加消息到会话，写入文件 |
| `get(conversationId)` | 返回最近 N 条消息（默认 10 条） |
| `getAll(conversationId)` | 返回全部消息 |
| `clear(conversationId)` | 删除会话文件 |

**线程安全**: 使用 `ThreadLocal<Kryo>` 确保 Kryo 实例线程隔离。

```java
private static final ThreadLocal<Kryo> kryoThreadLocal = ThreadLocal.withInitial(() -> {
    Kryo kryo = new Kryo();
    kryo.setRegistrationRequired(false);
    kryo.setInstantiatorStrategy(new StdInstantiatorStrategy());
    return kryo;
});
```

### 10.2 Redis 缓存

| 缓存键 | 用途 | 说明 |
|---|---|---|
| `login:user:{token}` | 用户登录态 | TTL: 60 分钟 |
| `chatMemory:{username}:{chatId}` | 会话最后活跃时间 | TTL: 24 小时 |
| `remove:chatMemory` | 待清理会话集合 | Set 类型，定时任务遍历 |

### 10.3 定时清理任务

**文件**: `src/main/java/com/kanodays88/skytakeoutai/timedTask/FileRemoveTask.java`

**执行周期**: `@Scheduled(cron = "0 0 */2 * * ?")` — 每 2 小时

**清理内容**:
1. 过期会话的 `.kryo` 文件
2. VectorStore 中对应会话的向量数据
3. Redis 中的过期缓存键

---

## 十一、工具系统

### 11.1 工具注册

**文件**: `src/main/java/com/kanodays88/skytakeoutai/config/ToolRegistration.java`

通过工厂模式统一注册：

```java
@Bean
public ToolCallback[] allTools() {
    return ToolCallbacks.from(
        dishTool,          // 菜品查询
        orderTool,         // 订单管理
        setmealTool,       // 套餐查询
        timeTool,          // 时间日期
        assignmentFinishTool, // 任务终止
        fileOperationTool,   // 文件操作
        pdfGenerationTool,   // PDF 生成
        resourceDownloadTool, // 资源下载
        webSearchTool        // 联网搜索
    );
}
```

### 11.2 工具列表

| 工具 | 功能 | 关键依赖 |
|---|---|---|
| `DishTool` | 菜品查询（分类/价格/名称筛选） | MyBatis-Plus, Redis, Redisson |
| `SetmealTool` | 套餐查询 | MyBatis-Plus |
| `OrderTool` | 订单生成/查询/删除 | MyBatis-Plus, 事务 |
| `TimeTool` | 获取当前日期时间 | Java 内置 |
| `WebSearchTool` | 联网搜索（百度 API + 向量 RAG 增强） | OkHttp, EmbeddingModel |
| `PDFGenerationTool` | PDF 文档生成 | iText |
| `FileOperationTool` | 文件读写 | HuTool |
| `ResourceDownloadTool` | URL 资源下载 | HuTool |
| `LoadReferenceTool` | 技能参考文档加载 | Spring Resource |
| `AssignmentFinishTool` | 任务终止标记 | 无（仅标记） |

### 11.3 按需分配

在 PlanExecute 中，每个子任务只获取所需的工具集：

```java
public ToolCallback[] getTools(Set<String> toolNames) {
    return Arrays.stream(allTools)
        .filter(t -> toolNames.contains(t.getToolDefinition().name())
                  || t.getToolDefinition().name().equals("assignmentFinish"))
        .toArray(ToolCallback[]::new);
}
```

`assignmentFinish` 工具始终被包含，确保 Agent 可以在任何子任务中标记完成。

---

## 十二、ChatClient 配置

### 12.1 主聊天客户端

**文件**: `src/main/java/com/kanodays88/skytakeoutai/config/ChatClientConfiguration.java`

```java
@Bean
public ChatClient chatClient(OpenAiChatModel model, ChatMemory chatMemory,
                             DishTool dishTool, SetmealTool setmealTool, OrderTool orderTool) {
    return ChatClient.builder(model)
        .defaultSystem(ChatSystem.CHAT_SYSTEM)   // 系统人设
        .defaultTools(dishTool, setmealTool, orderTool)  // 默认工具
        .defaultAdvisors(
            new MyLoggerAdvisor(),                          // 自定义日志切面
            MessageChatMemoryAdvisor.builder(chatMemory).build()  // 会话记忆增强
        )
        .build();
}
```

**MyLoggerAdvisor**: 自定义 CallAdvisor/StreamAdvisor 实现，记录每次 ChatClient 调用的用户请求和 AI 响应日志。

### 12.2 Agent 专用 ChatClient

RouterAgent、SimpleChatAgent、Kanodays88Manus 各自创建独立的 ChatClient 实例：

```java
ChatClient.builder(model)
    .defaultAdvisors(new MyLoggerAdvisor())
    .build();
```

不同实例使用不同的 tool 配置和 advisor 链，实现关注点隔离。

---

## 十三、完整调用链路一览

### 13.1 时序总结

```
用户                    拦截器                Controller             RouterAgent           执行引擎               LLM/工具/存储
 │                       │                     │                      │                    │                    │
 ├─GET /ai/chat/{msg}───►│                     │                      │                    │                    │
 │                       ├─RefreshTokenInterceptor                    │                    │                    │
 │                       │  ├─Redis获取用户信息─────────────────────────────────────────────► Redis            │
 │                       │  ├─刷新token─────────────────────────────────────────────────────► Redis            │
 │                       │  └─存入ThreadLocal     │                      │                    │                    │
 │                       ├─LoginInterceptor      │                      │                    │                    │
 │                       │  └─校验登录状态        │                      │                    │                    │
 │                       ├─────────────────────► │                      │                    │                    │
 │                       │                       ├─创建SseEmitter       │                    │                    │
 │                       │                       ├─捕获RequestContext   │                    │                    │
 │                       │                       ├─启动CompletableFuture│                    │                    │
 │                       │                       │                      │                    │                    │
 │                       │                       ├─异步线程开始─────────►│                    │                    │
 │                       │                       │  RouterAgent.route() │                    │                    │
 │                       │                       │  ├─selectSkills───►  │─────LLM调用───────► DeepSeek          │
 │                       │                       │  │                   │◄────返回技能列表────┤                    │
 │                       │                       │  ├─llmClassify───►   │─────LLM调用───────► DeepSeek          │
 │                       │                       │  │                   │◄────RouteDecision──┤                    │
 │◄────SSE:思考过程──────┤                       │  │                   │                    │                    │
 │                       │                       │  │                   │                    │                    │
 │                       │                       ├─SIMPLE_CHAT─────────►SimpleChatAgent      │                    │
 │                       │                       │  │  ├─加载历史记忆─────FileBasedChatMemory  │                    │
 │                       │                       │  │  ├─调用LLM─────────────────────────────► DeepSeek          │
 │                       │                       │  │  └─返回结果                              │                    │
 │                       │                       │  │                   │                    │                    │
 │                       │                       ├─COMPLEX_TASK────────►PlanExecute          │                    │
 │                       │                       │  │  ├─decomposeTask──►─────LLM调用───────► DeepSeek          │
 │                       │                       │  │  │◄────子任务列表──┤                    │                    │
 │                       │                       │  │  ├─buildWaves     │                    │                    │
 │                       │                       │  │  ├─并行执行Wave 1  │                    │                    │
 │                       │                       │  │  │  ├─Kanodays88Manus.run()             │                    │
 │                       │                       │  │  │  │  ├─think()──►─────LLM调用───────► DeepSeek          │
 │◄────SSE:思考过程──────┤                       │  │  │  │  │          │                    │                    │
 │                       │                       │  │  │  │  ├─act()────►工具调用────────────► DB/外部服务       │
 │                       │                       │  │  │  │  │          │                    │                    │
 │                       │                       │  │  │  │  └─结果蒸馏──►─────LLM调用───────► DeepSeek          │
 │                       │                       │  │  │  └─存储结果    │                    │                    │
 │                       │                       │  │  ├─并行执行Wave 2  │                    │                    │
 │                       │                       │  │  ├─...           │                    │                    │
 │                       │                       │  │  └─fuseResults──►─────LLM调用───────► DeepSeek          │
 │                       │                       │  │◄────最终结果──────┤                    │                    │
 │                       │                       │  │                   │                    │                    │
 │                       │                       ├─保存对话记忆─────────► FileBasedChatMemory                    │
 │                       │                       ├─更新Redis缓存────────────────────────────► Redis              │
 │◄────SSE:Agent结果─────┤                       │                      │                    │                    │
 │                       │                       │                      │                    │                    │
```

### 13.2 类调用关系图

```
ChatController
  ├── @Autowired OpenAiChatModel
  ├── @Autowired VectorStore
  ├── @Autowired ToolCallback[] (allTools)
  ├── @Autowired SkillRegistry
  ├── @Autowired PlanExecute
  │
  ├── → 创建 RouterAgent
  │     ├── → selectSkillsWithLLM() → LLM
  │     ├── → llmClassify() → LLM
  │     └── → 返回 RouteDecisionTotal
  │
  ├── → 创建 SimpleChatAgent
  │     ├── → chatMemory.get(chatId)
  │     └── → chatClient.prompt().call().content() → LLM
  │
  ├── → PlanExecute.planExecute()
  │     ├── → decomposeTaskWithContract() → LLM
  │     ├── → buildExecutionWaves()
  │     ├── → 创建 Kanodays88Manus (per subtask)
  │     │     └── → ToolCallAgent.think() → LLM
  │     │     └── → ToolCallAgent.act() → ToolCallingManager
  │     ├── → distillSubTaskResult() → LLM
  │     └── → fuseResults() → LLM
  │
  ├── → FileBasedChatMemory.add() — 保存对话记忆
  └── → StringRedisTemplate.opsForValue().set() — 缓存会话
```

---

## 十四、关键技术设计要点

### 14.1 线程上下文传递

**问题**: Spring AI 的请求处理在异步线程中执行，异步线程无法直接访问主 Web 线程的 `HttpServletRequest`。

**解决方案**:

```java
// 主线程：捕获上下文
RequestAttributes attributes = RequestContextHolder.getRequestAttributes();

// 子线程：恢复上下文
RequestContextHolder.setRequestAttributes(attributes);

// finally：清理
RequestContextHolder.resetRequestAttributes();
```

### 14.2 并发控制策略

| 场景 | 解决方案 |
|---|---|
| 同一 Wave 内任务并行 | 每个任务创建独立的 Agent 实例 + 独立线程池 |
| 共享结果存储 | `ConcurrentHashMap<Integer, DistilledResult>` |
| 波次同步 | `CompletableFuture.allOf().join()` |
| SSE 并发写入 | `synchronized(emitter)` |
| Kryo 线程安全 | `ThreadLocal<Kryo>` 线程隔离 |
| ThreadLocal 泄漏 | `finally` 块中 `remove()` |

### 14.3 上下文压缩（蒸馏）

**问题**: 多步 ReAct 循环 + 多工具调用导致上下文膨胀，Token 消耗大。

**解决方案**:

1. 阈值触发：结果 < 2000 字符跳过蒸馏
2. 智能压缩：LLM 删除无关内容，保留 `coreContent` 定义的必要字段
3. 双副本：压缩版（进上下文）+ 原版（兜底存档）
4. 典型压缩率：90%+

### 14.4 卡死检测与恢复

BaseAgent 在每次 step 执行后检测：

```java
if (isStuck()) {
    handleStuckState();  // 注入 "切换策略" 提示
}
```

检测逻辑：最近 5 条消息中有 ≥3 条相同 → 判定卡死 → 修改 `nextStepPrompt` 指导 LLM 换策略。

---

## 附录：关键配置说明

### application.yaml 核心配置

```yaml
spring:
  ai:
    openai:
      chat:
        base-url: https://api.deepseek.com          # DeepSeek API 地址
        api-key: ${DEEPSEEK_API_KEY}                 # API 密钥
        options:
          model: deepseek-v4-flash                   # 对话模型
          extra-body:
            thinking:
              type: disabled                         # 禁用思考模式
      embedding:
        base-url: https://dashscope.aliyuncs.com     # 阿里云百炼
        options:
          model: text-embedding-v4                   # 向量嵌入模型
          dimensions: 1024

  datasource:
    url: jdbc:mysql://192.168.74.100:3306/sky_take_out
    username: root

  redis:
    host: 192.168.74.100
    port: 6379
    database: 1
```

---

**文档版本**: v2.0  
**更新时间**: 2026-06-16  
**适用项目**: SkyTakeOut AI (Spring AI 智能外卖助手)
