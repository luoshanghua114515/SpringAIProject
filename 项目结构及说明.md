# 永雏塔菲小助手（SkyTakeOut AI）— 项目结构及说明

## 项目概述

基于 **Spring Boot 3.5 + Spring AI 1.1.6** 构建的 AI 智能外卖助手后端服务。项目以 B 站虚拟主播「永雏塔菲」为人设，通过 **LLM（DeepSeek V4 Flash）** 驱动多智能体协作系统，实现自然语言交互的外卖查询、下单、订单管理等功能，并支持联网搜索、PDF 生成/上传、RAG 知识库问答等扩展能力。

- **包名**: `com.kanodays88.skytakeoutai`
- **Java 版本**: 17
- **构建工具**: Maven
- **数据库**: MySQL + Redis + Redisson
- **AI 模型**: DeepSeek V4 Flash（对话）、text-embedding-v4（向量嵌入）

---

## 目录结构总览

```
AiProject/
├── pom.xml                                    # Maven 项目配置
├── mvnw / mvnw.cmd                            # Maven Wrapper
├── vectorStorePdf/                            # 向量数据库种子 PDF 文件
│   ├── 常见问题FAQ.pdf
│   └── 菜品口味信息.pdf
│
├── src/main/java/com/kanodays88/skytakeoutai/
│   ├── SkyTakeOutAiApplication.java           # 应用入口
│   │
│   ├── advisor/
│   │   └── MyLoggerAdvisor.java               # ChatClient 日志切面（请求/响应日志）
│   │
│   ├── agent/                                 # 智能体核心系统
│   │   ├── BaseAgent.java                     # 智能体基类（状态管理、循环执行、卡死检测）
│   │   ├── ReActAgent.java                    # ReAct 模式抽象（思考→行动循环）
│   │   ├── ToolCallAgent.java                 # 工具调用智能体（Think → Act 完整实现）
│   │   ├── Kanodays88Manus.java               # 主执行智能体（Manus 风格，5步最大）
│   │   ├── model/
│   │   │   └── AgentState.java                # 智能体状态枚举
│   │   ├── plan/
│   │   │   ├── PlanExecute.java               # 计划执行器（任务分解→并行执行→蒸馏→融合）
│   │   │   └── SubTask.java                   # 子任务数据模型（Record）
│   │   ├── router/
│   │   │   ├── RouterAgent.java               # 路由智能体（意图分类、Skill匹配）
│   │   │   ├── RouteDecision.java             # 路由决策记录
│   │   │   ├── RouteDecisionTotal.java        # 路由决策+匹配Skill
│   │   │   └── QuestionType.java              # 问题类型枚举
│   │   ├── simpleChat/
│   │   │   └── SimpleChatAgent.java           # 简单对话智能体
│   │   ├── rag/
│   │   │   └── RAGAgent.java                  # RAG 问答智能体
│   │   └── sse/
│   │       └── SSESend.java                   # SSE 事件推送工具
│   │
│   ├── common/
│   │   ├── ChatSystem.java                    # 系统提示词（永雏塔菲人设）
│   │   └── ChatDecide.java                    # 对话决策（RAG 相关性判断）
│   │
│   ├── config/
│   │   ├── ChatClientConfiguration.java       # ChatClient Bean 配置
│   │   ├── ToolRegistration.java              # 工具统一注册（工厂模式）
│   │   ├── MVCConfig.java                     # 拦截器注册 + 静态资源映射
│   │   ├── WebMvcConfiguration.java           # 静态资源映射补充
│   │   ├── RedisConfig.java                   # Redis+Lettuce 连接配置
│   │   └── RedissonConfig.java                # Redisson 分布式锁配置
│   │
│   ├── constant/
│   │   └── FileConstant.java                  # 文件存储路径常量
│   │
│   ├── content/
│   │   └── BaseContent.java                   # ThreadLocal 上下文（用户、会话ID）
│   │
│   ├── controller/
│   │   ├── ChatController.java                # AI 对话主控制器（SSE 流式响应）
│   │   ├── UserController.java                # 用户登录控制器
│   │   └── FileUploadController.java          # PDF 文件上传控制器
│   │
│   ├── documentLoad/
│   │   └── DocumentLoader.java                # Markdown 文档加载器
│   │
│   ├── entity/                                # 数据实体
│   │   ├── Category.java                      # 分类表
│   │   ├── Dish.java                          # 菜品表
│   │   ├── Setmeal.java                       # 套餐表
│   │   ├── SetmealDish.java                   # 套餐-菜品关联表
│   │   ├── Orders.java                        # 订单表
│   │   ├── OrderDetail.java                   # 订单明细表
│   │   ├── User.java                          # 用户表
│   │   ├── dto/
│   │   │   └── UserLoginDTO.java              # 用户登录 DTO
│   │   ├── vo/
│   │   │   ├── DishVO.java                    # 菜品视图对象
│   │   │   ├── SetmealVO.java                 # 套餐视图对象
│   │   │   ├── OrderVO.java                   # 订单视图对象
│   │   │   ├── UserLoginVO.java               # 登录响应 VO
│   │   │   └── FileUploadVO.java              # 文件上传响应 VO
│   │   └── querys/
│   │       ├── DishQuery.java                 # 菜品查询参数
│   │       ├── SetmealQuery.java              # 套餐查询参数
│   │       └── OrderQuery.java                # 订单生成参数
│   │
│   ├── interceptor/
│   │   ├── RefreshTokenInterceptor.java       # Token 刷新拦截器（全局 order=0）
│   │   └── LoginInterceptor.java              # 登录校验拦截器（order=1）
│   │
│   ├── mapper/                                # MyBatis-Plus 数据访问层
│   │   ├── CategoryMapper.java
│   │   ├── DishMapper.java
│   │   ├── SetmealMapper.java
│   │   ├── SetmealDishMapper.java
│   │   ├── OrdersMapper.java
│   │   ├── OrderDetailMapper.java
│   │   └── UserMapper.java
│   │
│   ├── memory/
│   │   └── FileBasedChatMemory.java           # 基于 Kryo 序列化的文件级对话记忆
│   │
│   ├── service/                               # 业务服务层
│   │   ├── CategoryService.java
│   │   ├── DishService.java
│   │   ├── SetmealService.java
│   │   ├── SetmealDishService.java
│   │   ├── OrdersService.java
│   │   ├── OrderDetailService.java
│   │   ├── UserService.java
│   │   ├── FileUploadService.java
│   │   └── impl/                              # 服务实现类
│   │       ├── CategoryServiceImpl.java
│   │       ├── DishServiceImpl.java
│   │       ├── SetmealServiceImpl.java
│   │       ├── SetmealDishServiceImpl.java
│   │       ├── OrdersServiceImpl.java
│   │       ├── OrderDetailServiceImpl.java
│   │       ├── UserServiceImpl.java
│   │       └── FileUploadServiceImpl.java
│   │
│   ├── skill/                                 # 业务技能系统（Anthropic Agent Skill 标准）
│   │   ├── Skill.java                         # 技能数据模型（参数表、执行流、工具等）
│   │   ├── SkillParameter.java                # 技能参数定义
│   │   ├── SkillStep.java                     # 技能执行步骤定义
│   │   ├── SkillLoader.java                   # 技能文件解析加载器（YAML frontmatter + 结构化 Section）
│   │   └── SkillRegistry.java                 # 技能注册中心（查找、匹配、生成上下文）
│   │
│   ├── timedTask/
│   │   └── FileRemoveTask.java                # 定时任务（每2小时清理过期会话和向量）
│   │
│   ├── tools/                                 # AI 工具集（Spring AI @Tool 注解）
│   │   ├── DishTool.java                      # 菜品查询（Redis + Redisson 缓存）
│   │   ├── SetmealTool.java                   # 套餐查询
│   │   ├── OrderTool.java                     # 订单生成/查询/删除（事务）
│   │   ├── TimeTool.java                      # 时间/日期工具
│   │   ├── WebSearchTool.java                 # 联网搜索（百度API + RAG 增强）
│   │   ├── PDFGenerationTool.java             # PDF 生成（iText）
│   │   ├── FileOperationTool.java             # 文件读写操作
│   │   ├── ResourceDownloadTool.java          # URL 资源下载
│   │   ├── LoadReferenceTool.java             # 技能参考文档加载
│   │   └── AssignmentFinishTool.java          # 任务终止标记工具
│   │
│   └── utils/
│       ├── CommonUtils.java                   # 通用工具类
│       ├── RedisUtils.java                    # Redis 操作工具
│       ├── FileScanUtils.java                 # 文件扫描工具
│       └── HttpPathUtil.java                  # HTTP 路径构建工具
│
├── src/main/resources/
│   ├── application.yaml                       # 主配置文件
│   ├── com/kanodays88/skytakeoutai/mapper/    # MyBatis XML 映射文件
│   │   ├── CategoryMapper.xml
│   │   ├── DishMapper.xml
│   │   ├── SetmealMapper.xml
│   │   ├── SetmealDishMapper.xml
│   │   ├── OrdersMapper.xml
│   │   ├── OrderDetailMapper.xml
│   │   └── UserMapper.xml
│   └── skills/                                # 业务技能 Markdown 定义
│       ├── food_delivery/
│       │   ├── query_dish_or_setmeal/
│       │   │   ├── SKILL.md                   # 菜品/套餐查询技能
│       │   │   └── references/
│       │   │       └── dish-flavor.md         # 菜品口味参考数据
│       │   ├── place_order/
│       │   │   └── SKILL.md                   # 下单技能
│       │   ├── cancel_order/
│       │   │   └── SKILL.md                   # 取消订单技能
│       │   └── query_order/
│       │       └── SKILL.md                   # 查询订单技能
│       └── tarvel_plan/
│           └── make_tarvel_plan/
│               └── SKILL.md                   # 旅游行程规划技能
│
├── src/test/java/                             # 测试目录
│   ├── SkyTakeOutAiApplicationTests.java
│   └── tools/WebSearchToolTest.java
```

---

## 核心架构设计

### 1. 智能体系统（Agent System）

采用分层智能体架构，按复杂度递进：

```
RouterAgent (路由智能体)
  ├── 意图分类: SIMPLE_CHAT / COMPLEX_TASK / AMBIGUOUS
  ├── Skill 匹配（基于 LLM 语义选择）
  │
  ├── SIMPLE_CHAT → SimpleChatAgent (简单对话)
  │
  ├── COMPLEX_TASK → PlanExecute (计划执行器)
  │     └── 任务分解 → 并行 Wave 执行
  │           └── Kanodays88Manus (主执行智能体)
  │                 └── ToolCallAgent (工具调用)
  │                       └── ReActAgent (思考→行动循环)
  │                             └── BaseAgent (基类)
  │
  └── AMBIGUOUS → 反问用户补充信息
```

**类继承链**：`BaseAgent` → `ReActAgent` → `ToolCallAgent` → `Kanodays88Manus`

| 类 | 职责 |
|---|---|
| `BaseAgent` | 状态管理（IDLE/RUNNING/FINISHED/ERROR）、最大步数控制、卡死检测、循环执行骨架 |
| `ReActAgent` | 定义 `think()` + `act()` 抽象，实现 `step()` 方法编排思考→行动 |
| `ToolCallAgent` | 完整 LLM 工具调用实现，禁用 Spring AI 自动工具执行，手动编排 ToolCallingManager |
| `Kanodays88Manus` | 具体配置（5步、工具集、ChatClient），作为最终执行单元 |

### 2. 计划执行系统（Plan Execute）

`PlanExecute` 是复杂任务的执行引擎：

1. **任务分解**：将用户目标分解为多个带依赖契约的 `SubTask`（LLM 驱动）
2. **Wave 构建**：根据依赖关系构建并行执行波次
3. **并行执行**：每个 Wave 内子任务多线程并行执行，Kanodays88Manus 驱动每个子任务
4. **结果蒸馏**：对过长结果进行 LLM 蒸馏压缩，保留核心内容
5. **结果融合**：将全部子任务的蒸馏结果融合为最终输出

### 3. 业务技能系统（Skill System）

基于 Anthropic Agent Skill 标准的可插拔技能系统：

- **定义文件**：`src/main/resources/skills/**/SKILL.md`（Markdown + YAML frontmatter）
- **核心组件**：
  - `SkillLoader`：解析 Markdown 文件，提取 frontmatter、参数表、执行流、工具列表
  - `SkillRegistry`：启动时加载所有技能，提供语义匹配查找
- **使用场景**：
  - `RouterAgent` 根据用户输入匹配技能，判断信息完整性 → 决定 `AMBIGUOUS`
  - `PlanExecute` 将匹配技能注入任务分解 prompt，指导子任务生成

**已定义的业务技能**：

| 技能 | 领域 | 说明 |
|---|---|---|
| `dish_and_setmeal_query` | food_delivery | 查询菜品/套餐，支持口味、价格、分类筛选 |
| `place_order` | food_delivery | 外卖下单（收集信息→确认→生成订单） |
| `cancel_order` | food_delivery | 取消订单（查询→确认→删除） |
| `query_order` | food_delivery | 查询订单信息（手机号/订单号） |
| `general_trip_planner` | travel | 旅游行程规划（联网搜索→生成文件） |

### 4. 对话路由流程

```
用户请求 → RefreshTokenInterceptor（token 刷新）
         → LoginInterceptor（登录校验）
         → ChatController.serviceChat()（SSE 流式接口）
              → RouterAgent.route()（意图分析）
                   ├─ SIMPLE_CHAT → SimpleChatAgent.simpleChat()
                   ├─ COMPLEX_TASK → PlanExecute.planExecute()
                   └─ AMBIGUOUS → 反问用户
              → 保存对话记忆（Kryo 文件序列化）
              → Redis 缓存会话有效期（24h）
```

### 5. AI 工具集（10 个工具）

| 工具 | 功能 | 关键依赖 |
|---|---|---|
| `DishTool` | 菜品查询（支持分类/价格/名称筛选，Redis+Redisson 缓存） | MyBatis-Plus, Redis, Redisson |
| `SetmealTool` | 套餐查询 | MyBatis-Plus |
| `OrderTool` | 生成/查询/删除订单（事务管理） | MyBatis-Plus, MySQL |
| `TimeTool` | 获取当前日期时间 | Java 内置 |
| `WebSearchTool` | 联网搜索（百度 AI Search API + 向量 RAG 增强） | OkHttp, EmbeddingModel |
| `PDFGenerationTool` | 生成 PDF 文档（支持中文、图片） | iText |
| `FileOperationTool` | 文件读写操作 | HuTool |
| `ResourceDownloadTool` | 从 URL 下载资源 | HuTool |
| `LoadReferenceTool` | 加载技能参考文档 | Spring Resource |
| `AssignmentFinishTool` | 任务终止标记 | — |

### 6. 数据持久化

| 存储 | 用途 | 技术 |
|---|---|---|
| **MySQL** | 业务数据（菜品、套餐、订单、用户、分类） | MyBatis-Plus |
| **Redis** | Token 登录态、菜品查询缓存、会话有效期管理 | Spring Data Redis + Lettuce |
| **Redisson** | 分布式锁（缓存击穿防护） | Redisson |
| **文件系统 (Kryo)** | 对话历史记忆（每个会话一个 `.kryo` 文件） | Kryo 序列化 |
| **SimpleVectorStore** | RAG 向量检索（内存级，用于 PDF 问答 + 搜索增强） | Spring AI VectorStore |

### 7. 定时任务

- **`FileRemoveTask`**：每 2 小时扫描 Redis 中过期的会话记录，清理：
  - 文件系统中的会话文件和记忆文件
  - VectorStore 中对应会话的向量数据
  - Redis 中的缓存键

### 8. 关键配置

| 配置项 | 说明 |
|---|---|
| `spring.ai.openai.chat.base-url` | https://api.deepseek.com（兼容 OpenAI SDK） |
| `spring.ai.openai.chat.options.model` | deepseek-v4-flash（主要对话模型） |
| `spring.ai.openai.embedding.options.model` | text-embedding-v4（阿里云百炼） |
| `spring.datasource.url` | MySQL 连接（192.168.74.100:3306/sky_take_out） |
| `spring.redis.host` | Redis 连接（192.168.74.100:6379） |

---

## 关键技术亮点

1. **双拦截器链**：`RefreshTokenInterceptor`(order=0) 全局刷新 token → `LoginInterceptor`(order=1) 校验登录态，避免 token 过期
2. **SSE 流式推送**：通过 `SseEmitter` 将智能体的思考过程和最终结果实时推送给前端
3. **线程上下文传递**：异步执行时通过 `RequestContextHolder` 传递 Web 请求上下文到子线程
4. **卡死检测**：`BaseAgent.isStuck()` 检测 LLM 重复输出，自动注入"切换策略"提示
5. **并行 Wave 执行**：PlanExecute 根据子任务依赖图构建并行执行波次，最大化执行效率
6. **结果蒸馏**：对超过 2000 字符的子任务结果进行 LLM 蒸馏，节约上下文 Token
7. **RAG 搜索增强**：WebSearchTool 对搜索结果做向量检索提纯，只保留最相关内容
